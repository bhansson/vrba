<section class="py-20 px-6 lg:px-8 bg-brand-gradient-light">
    <div class="mx-auto max-w-4xl text-center">
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 tracking-tight">
            Ready to transform your catalog?
        </h2>

        <p class="mt-4 text-xl text-gray-600">
            Start free. No credit card required.
        </p>

        <!-- CTA Buttons -->
        <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
            <a href="{{ route('register') }}" class="inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-white bg-brand-gradient rounded-lg hover:scale-105 transition-transform duration-200 shadow-lg">
                Get Started Free
            </a>
            <a href="{{ route('login') }}" class="inline-flex items-center justify-center px-8 py-4 text-lg font-medium text-gray-700 hover:text-gray-900 transition-colors">
                Already have an account? <span class="ml-1 font-semibold">Log in</span>
            </a>
        </div>

        <!-- Trust Element -->
        <p class="mt-8 text-sm text-gray-500">
            Join e-commerce teams managing 500K+ products with Magnifiq
        </p>
    </div>
</section>
